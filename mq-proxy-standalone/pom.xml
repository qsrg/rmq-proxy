<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mq.proxy</groupId>
        <artifactId>mq-proxy-system</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mq-proxy-standalone</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.mq.proxy</groupId>
            <artifactId>mq-proxy-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        
        <dependency>
            <groupId>com.mq.proxy</groupId>
            <artifactId>mq-proxy-rocketmq</artifactId>
            <version>${project.version}</version>
        </dependency>
        
        <dependency>
            <groupId>com.mq.proxy</groupId>
            <artifactId>mq-proxy-mock</artifactId>
            <version>${project.version}</version>
        </dependency>
        
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
        </dependency>
        
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-client</artifactId>
        </dependency>
        
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
        
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.mq.proxy.core.ProxyStartup</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/mq-proxy/lib</outputDirectory>
                            <excludeScope>provided</excludeScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.8</version>
                <executions>
                    <execution>
                        <id>prepare-distribution</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/mq-proxy"/>
                                <mkdir dir="${project.build.directory}/mq-proxy/bin"/>
                                <mkdir dir="${project.build.directory}/mq-proxy/conf"/>
                                <mkdir dir="${project.build.directory}/mq-proxy/logs"/>

                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      todir="${project.build.directory}/mq-proxy/lib"/>
                                <copy file="${project.basedir}/bin/proxy.sh"
                                      todir="${project.build.directory}/mq-proxy/bin"/>
                                <copy file="${project.basedir}/src/main/resources/proxy.properties"
                                      todir="${project.build.directory}/mq-proxy/conf"/>
                                <copy file="${project.basedir}/src/main/resources/logback.xml"
                                      todir="${project.build.directory}/mq-proxy/conf"/>

                                <tar destfile="${project.build.directory}/mq-proxy-${project.version}.tar.gz"
                                     compression="gzip">
                                    <tarfileset dir="${project.build.directory}/mq-proxy"
                                                prefix="mq-proxy-${project.version}">
                                        <include name="bin/**"/>
                                        <exclude name="bin/*.sh"/>
                                    </tarfileset>
                                    <tarfileset dir="${project.build.directory}/mq-proxy"
                                                prefix="mq-proxy-${project.version}"
                                                filemode="755">
                                        <include name="bin/*.sh"/>
                                    </tarfileset>
                                    <tarfileset dir="${project.build.directory}/mq-proxy"
                                                prefix="mq-proxy-${project.version}">
                                        <include name="conf/**"/>
                                        <include name="lib/**"/>
                                        <include name="logs/**"/>
                                    </tarfileset>
                                </tar>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
